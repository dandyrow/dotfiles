# Unslop

Apply the `unslop` skill before finalizing any prose an agent writes — code
comments, commit messages, PR/issue descriptions, ticket comments, chat replies.

The skill is the authority for the full pattern list; apply it on every output.
